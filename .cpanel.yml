---
deployment:
  tasks:
    - export DEPLOYPATH=/home2/gmussyco/public_html/anti-mev-dex
    - /bin/cp -R build/static $DEPLOYPATH
    - /bin/cp -R build/static/css $DEPLOYPATH
    - /bin/cp -R build/static/js $DEPLOYPATH
    - /bin/cp build /*.* $DEPLOYPATH
    - /bin/cp build /*.* $DEPLOYPATH
